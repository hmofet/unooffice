# UnoWord: open a document from a folder that is NOT the first volume, edit
# it, and plain-Save it: it must go back to that folder, not to volume 0
# (the pre-0.2 apps saved every plain Save to volume 0).
# `pick` answers the OS file dialog, which a script cannot drive.
pick other/Letter.doc
key o ctrl
frames 5
shot word_reopened.ppm
text  Edited.
key s ctrl
frames 5
shot word_resaved.ppm
key Escape
